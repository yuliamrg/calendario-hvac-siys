{
  "name": "calendario-hvac-siys",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Calendario local autocontenido para programación de servicios HVAC SI&S.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run test && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
